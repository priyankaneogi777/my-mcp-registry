{
  "name": "personal-mcp-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Personal MCP Registry with dynamic SQL extraction & 1-click token links",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}